{
  "name": "@zunialab/backend",
  "version": "0.0.0",
  "private": true,
  "description": "Zunia backend API scaffold (notifications, push, indexer proxy)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json",
    "dev": "echo \"Implement API server after ADR + indexer choice\"",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
